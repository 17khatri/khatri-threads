"use client";

import { useEffect, useState } from "react";
import { FiEdit2, FiPlus, FiTrash2 } from "react-icons/fi";
import Button from "@/app/components/button";
import { Modal } from "@/app/components/modal";
import { Checkbox, FormField, Input, Textarea } from "@/app/components/form-fields";
import { H2, H3, P } from "@/app/components/typography";

type Master = { id: string; name: string }; type Product = { id: string; name: string; sku: string | null; price: string; status: "DRAFT" | "ACTIVE" | "ARCHIVED"; description: string | null; isFeatured: boolean; categoryId: string; collectionId: string | null; category: Master; collection: Master | null; images: { url: string }[] };
const blank = { name: "", sku: "", price: "", description: "", categoryId: "", collectionId: "", status: "DRAFT", isFeatured: false, images: "" };
export default function ProductsPage() {
  const [products, setProducts] = useState<Product[]>([]); const [categories, setCategories] = useState<Master[]>([]); const [collections, setCollections] = useState<Master[]>([]); const [form, setForm] = useState(blank); const [editing, setEditing] = useState<Product | null>(null); const [open, setOpen] = useState(false); const [error, setError] = useState(""); const [saving, setSaving] = useState(false);
  const load = async () => { const [productResponse, categoryResponse, collectionResponse] = await Promise.all([fetch("/api/products", { cache: "no-store" }), fetch("/api/categories", { cache: "no-store" }), fetch("/api/collections", { cache: "no-store" })]); if (productResponse.ok) setProducts(await productResponse.json()); if (categoryResponse.ok) setCategories(await categoryResponse.json()); if (collectionResponse.ok) setCollections(await collectionResponse.json()); };
  useEffect(() => {
    const timer = window.setTimeout(() => void load(), 0);
    return () => window.clearTimeout(timer);
  }, []);
  const change = (key: keyof typeof blank, value: string | boolean) => setForm((current) => ({ ...current, [key]: value }));
  const openCreate = () => { setEditing(null); setForm(blank); setError(""); setOpen(true); };
  const openEdit = (product: Product) => { setEditing(product); setForm({ name: product.name, sku: product.sku ?? "", price: product.price, description: product.description ?? "", categoryId: product.categoryId, collectionId: product.collectionId ?? "", status: product.status, isFeatured: product.isFeatured, images: product.images.map((image) => image.url).join("\n") }); setError(""); setOpen(true); };
  const save = async (event: React.FormEvent) => { event.preventDefault(); setSaving(true); setError(""); const response = await fetch(editing ? `/api/products/${editing.id}` : "/api/products", { method: editing ? "PUT" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...form, images: form.images.split("\n") }) }); const data = await response.json(); setSaving(false); if (!response.ok) return setError(data.error); setOpen(false); void load(); };
  const remove = async (id: string) => { if (!confirm("Delete this product?")) return; const response = await fetch(`/api/products/${id}`, { method: "DELETE" }); if (!response.ok) return setError((await response.json()).error); void load(); };
  return <div className="panel list-panel"><div className="panel-heading"><div><H2 className="mb-2">Products</H2><P>Add products, prices, images, categories, and optional collections.</P></div><Button onClick={openCreate}><FiPlus /> Add Product</Button></div>{error && <p className="form-error">{error}</p>}<div className="overflow-x-auto"><div className="min-w-[700px] divide-y divide-line">{products.length ? products.map((product) => <article key={product.id} className="grid grid-cols-[2fr_1fr_1fr_1fr_auto] items-center gap-4 py-4"><div><H3>{product.name}</H3><p className="mt-1 text-sm text-black/60">{product.sku ? `SKU: ${product.sku}` : "No SKU"}</p></div><p className="text-sm">₹ {Number(product.price).toLocaleString("en-IN")}</p><p className="text-sm">{product.category.name}</p><p className="text-sm"><span className="rounded-full bg-black/5 px-3 py-1">{product.status}</span></p><div className="item-actions"><button aria-label={`Edit ${product.name}`} onClick={() => openEdit(product)}><FiEdit2 size={16} /></button><button aria-label={`Delete ${product.name}`} onClick={() => void remove(product.id)}><FiTrash2 color="red" size={16} /></button></div></article>) : <div className="surface-message">No products found.</div>}</div></div><Modal open={open} title={editing ? "Edit Product" : "Add Product"} onClose={() => setOpen(false)}><form onSubmit={save} className="grid gap-3"><div className="grid gap-3 sm:grid-cols-2"><FormField label="Product Name" required><Input value={form.name} onChange={(e) => change("name", e.target.value)} required /></FormField><FormField label="SKU"><Input value={form.sku} onChange={(e) => change("sku", e.target.value)} /></FormField><FormField label="Price (₹)" required><Input type="number" min="0" step="0.01" value={form.price} onChange={(e) => change("price", e.target.value)} required /></FormField><FormField label="Category" required><select className="w-full rounded-xl border border-gray-200 bg-white px-4 py-3" value={form.categoryId} onChange={(e) => change("categoryId", e.target.value)} required><option value="">Select category</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></FormField><FormField label="Collection"><select className="w-full rounded-xl border border-gray-200 bg-white px-4 py-3" value={form.collectionId} onChange={(e) => change("collectionId", e.target.value)}><option value="">No collection</option>{collections.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></FormField><FormField label="Status"><select className="w-full rounded-xl border border-gray-200 bg-white px-4 py-3" value={form.status} onChange={(e) => change("status", e.target.value)}><option value="DRAFT">Draft</option><option value="ACTIVE">Active</option><option value="ARCHIVED">Archived</option></select></FormField></div><FormField label="Description"><Textarea rows={3} value={form.description} onChange={(e) => change("description", e.target.value)} /></FormField><FormField label="Image URLs"><Textarea rows={3} value={form.images} onChange={(e) => change("images", e.target.value)} placeholder="One image URL per line" /></FormField><Checkbox label="Feature this product on the home page" checked={form.isFeatured} onChange={(e) => change("isFeatured", e.target.checked)} />{error && <p className="form-error">{error}</p>}<div className="flex justify-end"><Button disabled={saving}>{saving ? "Saving..." : editing ? "Update Product" : "Create Product"}</Button></div></form></Modal></div>;
}
